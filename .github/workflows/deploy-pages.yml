name: Deploy Docs to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PNPM
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Build (Next 15 static export)
        env:
          NEXT_PUBLIC_SITE_ORIGIN: https://Dendro-X0.github.io
          NEXT_BASE_PATH: /opendeploy-cli-docs-site
          NEXT_PUBLIC_BASE_PATH: /opendeploy-cli-docs-site
          NEXT_PUBLIC_OPD_VERSION: v1.1.1
          NEXT_EXPORT: '1'
        run: pnpm build

      - name: Export (fallback if out/ missing)
        env:
          NEXT_BASE_PATH: /opendeploy-cli-docs-site
          NEXT_PUBLIC_BASE_PATH: /opendeploy-cli-docs-site
          NEXT_PUBLIC_OPD_VERSION: v1.1.1
          NEXT_EXPORT: '1'
        run: |
          if [ ! -d out ]; then
            pnpm exec next export -o out
          fi

      - name: Add .nojekyll
        run: echo > out/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
